1. код разный потому что решала не по порядку и по ходу работы мне приходили другие идеи решения
2. использовала один и тот же код для перевода строки в нужный тип
3. в первых задачах нет комментариев т. к. код банален
4. не комментировала каждую строку ибо объяснила только основные биты алгоритма
